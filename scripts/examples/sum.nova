# Nova example: Iterative sum
# Demonstrates loops and mutable variables

fn sum(n: i64) -> i64 {
    var total: i64 = 0;
    var i: i64 = 0;
    while i <= n {
        total = total + i;
        i = i + 1;
    }
    return total;
}

fn main() -> i64 {
    return sum(10);  // Should return 55
}