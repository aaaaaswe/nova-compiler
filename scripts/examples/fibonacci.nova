# Nova example: Fibonacci sequence
# Demonstrates basic Nova language features

fn fib(n: i64) -> i64 {
    if n <= 1 {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

fn main() -> i64 {
    return fib(10);  // Should return 55
}